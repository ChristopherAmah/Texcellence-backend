const isEmail = (value) => /^\S+@\S+\.\S+$/.test(value);
const requiredText = (body, field, maxLength = 254) => typeof body[field] !== 'string' || !body[field].trim() ? `${field} is required.` : body[field].trim().length > maxLength ? `${field} is too long.` : null;
export const registerValidator = (body) => { for (const field of ['firstName', 'lastName']) { const error = requiredText(body, field, 80); if (error) return { valid: false, field, message: error }; } if (!isEmail(body.email)) return { valid: false, field: 'email', message: 'A valid email is required.' }; if (typeof body.password !== 'string' || body.password.length < 12) return { valid: false, field: 'password', message: 'Password must be at least 12 characters.' }; return { valid: true }; };
export const loginValidator = (body) => !isEmail(body.email) ? { valid: false, field: 'email', message: 'A valid email is required.' } : typeof body.password !== 'string' || !body.password ? { valid: false, field: 'password', message: 'Password is required.' } : { valid: true };
